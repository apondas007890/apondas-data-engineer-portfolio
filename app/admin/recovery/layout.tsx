import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Account Recovery | Apon Kumar Das",
}

export default function AdminRecoveryLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
